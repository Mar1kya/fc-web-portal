image: node:20-alpine

stages:
  - lint
  - type-check
  - build
  - mirror

cache:
  key:
    files:
      - package-lock.json
    prefix: v2
  paths:
    - .npm/

lint_job:
  stage: lint
  rules:
    - if: $CI_COMMIT_BRANCH 
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - npm ci --cache .npm --prefer-offline --ignore-scripts
    - npx prisma generate
    - npm run lint

typecheck_job:
  stage: type-check
  rules:
    - if: $CI_COMMIT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - npm ci --cache .npm --prefer-offline --ignore-scripts
    - npx prisma generate
    - npm run type-check

build_job:
  stage: build
  rules:
    - if: $CI_COMMIT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - npm ci --cache .npm --prefer-offline --ignore-scripts
    - npx prisma generate
    - npm run build

mirror_to_github:
  stage: mirror
  image: alpine/git
  rules:
    - if: $CI_COMMIT_BRANCH == "main" 
  script:
    - git config --global user.email "marian0412510301@gmail.com"
    - git config --global user.name "Mar1kya"
    - git remote add github https://${GITHUB_USER}:${GITHUB_TOKEN}@github.com/${GITHUB_USER}/${GITHUB_REPO}.git
    - git push github HEAD:refs/heads/main --force